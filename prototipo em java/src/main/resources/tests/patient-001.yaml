testId: Patient-001
description: "Validação de paciente sem nome"
context:
  profiles:
    - "http://hl7.org/fhir/StructureDefinition/Patient"
instancePath: "src/main/resources/tests/instances/patient.json"
expectedResults:
  status: error
  errors:
    - "Patient.name: minimum required = 1, but only found 0"